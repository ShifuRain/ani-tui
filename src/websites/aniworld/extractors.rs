/// VOE hoster extractor: resolves a `/redirect/<id>` link into a playable stream link.
pub mod voe;
/// Vidmoly hoster extractor: resolves a `/redirect/<id>` link into a playable stream link.
pub mod vidmoly;
